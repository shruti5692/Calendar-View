import { makeStyles } from "@fluentui/react-components";

export const calendarViewStyles = makeStyles({
  container: {
    width: "100%",
    maxWidth: "1200px",
    margin: "auto",
    backgroundColor: "#fff",
    boxShadow: "0px 2px 4px rgba(0,0,0,0.1)",
    borderRadius: "8px",
    padding: "16px",
  },
});
