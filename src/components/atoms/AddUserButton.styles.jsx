import { makeStyles } from "@fluentui/react-components";

export const addUserButtonStyles = makeStyles({
  root: {
    backgroundColor: "#28a745",
    color: "#fff",
    padding: "8px 12px",
    borderRadius: "4px",
    ":hover": {
      backgroundColor: "#218838",
    },
  },
});
