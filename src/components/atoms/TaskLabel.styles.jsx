import { makeStyles } from "@fluentui/react-components";

export const taskLabelStyles = makeStyles({
  root: {
    padding: "4px 8px",
    backgroundColor: "#FFD700",
    borderRadius: "4px",
    fontSize: "12px",
    fontWeight: "bold",
  },
});
