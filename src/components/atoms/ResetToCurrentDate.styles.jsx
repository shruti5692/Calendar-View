import { makeStyles } from "@fluentui/react-components";

export const resetButtonStyles = makeStyles({
  root: {
    backgroundColor: "#0078D4",
    color: "#fff",
    padding: "8px 16px",
    borderRadius: "4px",
    cursor: "pointer",
    ":hover": {
      backgroundColor: "#005A9E",
    },
  },
});
